<!-- Generated by scripts/docs/export_public_api.py — do not edit by hand. -->

# Public API reference

Stable symbols re-exported from `srxy` (`srxy.__all__`). Regenerate with:

```bash
python scripts/docs/export_public_api.py
```


## `ActivityUpdate`

Dataclass (`srxy.domain.progress.ActivityUpdate`)

ActivityUpdate(label: 'str | None', current: 'int | None' = None, total: 'int | None' = None)

```python
ActivityUpdate(label: str | None, current: int | None = None, total: int | None = None)
```

Fields:

- `label`: `str | None`
- `current`: `int | None`
- `total`: `int | None`

## `FieldConfig`

Dataclass (`srxy.domain.models.FieldConfig`)

FieldConfig(name: 'str', match: 'MatchType' = <MatchType.COMPOSITE: 'composite'>, weight: 'float' = 1.0, composite_weights: 'dict[MatchType, float] | None' = None)

```python
FieldConfig(name: str, match: MatchType = MatchType.COMPOSITE, weight: float = 1.0, composite_weights: dict[MatchType, float] | None = None)
```

Fields:

- `name`: `str`
- `match`: `MatchType`
- `weight`: `float`
- `composite_weights`: `dict[MatchType, float] | None`

## `FileQ`

Dataclass (`srxy.domain.file_query.FileQ`)

Boolean search expression over fuzzy terms (always composite matching).

```python
FileQ(node_type: QNodeType, value: str | None = None, children: tuple[FileQ, ...] = ())
```

Fields:

- `node_type`: `QNodeType`
- `value`: `str | None`
- `children`: `tuple[FileQ, ...]`

## `FileSearchResult`

Dataclass (`srxy.domain.models.FileSearchResult`)

FileSearchResult(path: 'Path', score: 'float', breakdown: 'dict[str, float]' = <factory>, lines: 'list[LineMatch]' = <factory>, term_surfaces: 'dict[str, dict[str, float]]' = <factory>)

```python
FileSearchResult(path: Path, score: float, breakdown: dict[str, float] = ..., lines: list[LineMatch] = ..., term_surfaces: dict[str, dict[str, float]] = ...)
```

Fields:

- `path`: `Path`
- `score`: `float`
- `breakdown`: `dict[str, float]`
- `lines`: `list[LineMatch]`
- `term_surfaces`: `dict[str, dict[str, float]]`

## `LineMatch`

Dataclass (`srxy.domain.models.LineMatch`)

LineMatch(line_number: 'int', text: 'str', score: 'float', location_kind: 'str' = 'line', matched_term: 'str | None' = None)

```python
LineMatch(line_number: int, text: str, score: float, location_kind: str = 'line', matched_term: str | None = None)
```

Fields:

- `line_number`: `int`
- `text`: `str`
- `score`: `float`
- `location_kind`: `str`
- `matched_term`: `str | None`

## `MatchType`

Enum (`srxy.domain.models.MatchType`)

Members:

- `EXACT` = `'exact'`
- `CONTAINS` = `'contains'`
- `PARTIAL` = `'partial'`
- `FUZZY` = `'fuzzy'`
- `PHONETIC` = `'phonetic'`
- `SEMANTIC` = `'semantic'`
- `COMPOSITE` = `'composite'`

## `Q`

Dataclass (`srxy.domain.models.Q`)

Composable query expression tree for field-level search logic.

```python
Q(field: str, *, match: MatchType = MatchType.COMPOSITE, weight: float = 1.0, composite_weights: dict[MatchType, float] | None = None)
```

Fields:

- `node_type`: `QNodeType`
- `field`: `str | None`
- `match`: `MatchType`
- `weight`: `float`
- `composite_weights`: `dict[MatchType, float] | None`
- `children`: `tuple[Q, ...]`

## `SearchResult`

Dataclass (`srxy.domain.models.SearchResult`)

SearchResult(item: 'Any', score: 'float', breakdown: 'dict[str, Any]' = <factory>)

```python
SearchResult(item: Any, score: float, breakdown: dict[str, Any] = ...)
```

Fields:

- `item`: `Any`
- `score`: `float`
- `breakdown`: `dict[str, Any]`

## `SkippedFile`

Dataclass (`srxy.domain.models.SkippedFile`)

SkippedFile(path: 'Path', size_bytes: 'int', reason: 'str' = 'oversized')

```python
SkippedFile(path: Path, size_bytes: int, reason: str = 'oversized')
```

Fields:

- `path`: `Path`
- `size_bytes`: `int`
- `reason`: `str`

## `magic_file_search`

```python
magic_file_search(path: Path | str, query: str | FileQ, *, search_names: bool = True, search_contents: bool = True, search_docs_tags: bool = True, threshold: float = 0.35, max_file_size: int | None = None, max_matches: int = 50, skip_hidden_folders: bool = True, skip_noise_folders: bool = True, include_archives: bool = False, include_subdirectories: bool = True, skipped_files: list[SkippedFile] | None = None, ocr: bool | None = None, transcribe: bool | None = None, semantic_image: bool | None = None, semantic_image_threshold: float = 0.18, transcribe_threshold: float = 0.25, limit: int | None = None, on_progress: Callable[[int, int], None] | None = None, on_activity: ActivityCallback | None = None, on_result: Callable[[FileSearchResult], None] | None = None, max_line_matches: int | None = None, max_workers: int | None = None) -> list[FileSearchResult]
```

Public library facade — builds a default ``FileSearchUseCase``.

## `magic_search`

```python
magic_search(items: list[Any], query: str, *, fields: list[str] = [], threshold: float = 0.35) -> list[SearchResult]
```

## `search`

```python
search(items: list[Any], query: str, *, fields: list[FieldConfig] | None = None, where: Q | None = None, threshold: float = 0.0, require_all: bool = False) -> list[SearchResult]
```
