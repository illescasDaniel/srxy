<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
	Hardened runtime is required for notarization, but the offline installer's
	.app bundles a managed CPython (python-build-standalone) plus PySide6/Qt
	frameworks that are not signed with our Developer ID. Without disabling
	library validation, the signed CPython binary would refuse to dlopen its
	own extension modules (.so) and Qt frameworks at runtime.
	-->
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.allow-dyld-environment-variables</key>
	<true/>
</dict>
</plist>
